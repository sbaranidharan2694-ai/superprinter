# Phase 1 — Technical SEO Audit Baseline

Generated by ruflo swarm `swarm-1779831001721-hdcpzp`, agent `agent-1780060204479-gvwfj1`.

## Status by category

| # | Check | Status | Evidence |
|---|---|---|---|
| 1 | Crawlability / indexability | ✅ COVERED | `public/robots.txt:1-62` allows all major crawlers; `Disallow: /orders` only. `public/sitemap.xml` enumerates ~80 URLs. `index.html:39-40` emits `index, follow, max-snippet:-1, max-image-preview:large`. GSC verification both via meta tag and HTML file. No `noindex` leaks. |
| 2 | Site speed / CWV | ✅ COVERED (qualitative — needs field measurement) | AVIF preload + WebP logo with `fetchpriority="high"` (`index.html:52-53`). Two-tier font strategy (`index.html:62-66`) — Playfair 700 blocking, rest deferred. GA4 interaction/idle-deferred. Hero H1 static SSR (no opacity:0). Picture element with AVIF/WebP/JPG fallback + explicit width/height (CLS-safe). Critical inline CSS for header. |
| 3 | Mobile-friendliness | ✅ COVERED | Viewport `width=device-width, initial-scale=1.0` — no `maximum-scale` anti-pattern. Responsive Tailwind everywhere. Mobile menu = role=dialog with focus trap + Esc. Tap targets ≥ 44×44. |
| 4 | Schema markup | ✅ COVERED | Sitewide LocalBusiness/PrintShop/ProfessionalService with address/telephone/openingHoursSpecification/geo/contactPoint[]/areaServed[]/sameAs[]/hasOfferCatalog (10 priced Offers)/award/knowsAbout/founder/numberOfEmployees/taxID. Per-page WebPage + Speakable + FAQPage + Review + Product + Service + ItemList + BreadcrumbList. |
| 5 | Site architecture | ✅ COVERED | 60+ routes. Top nav: Services, Products, Weddings, Gallery, About, Contact + Get Quote CTA + WhatsApp + phone. 30 suburb pages, 6 industry verticals, 3 head-keyword pages. Hub-and-spoke via /services and /products. |

## Owner actions

1. Run **PageSpeed Insights** + **CrUX** on the live URL — qualitative posture is excellent, but only field LCP / INP / CLS data will confirm.
2. Submit `https://superprinters.net/sitemap.xml` in Google Search Console.
3. Request indexing for the top 30 URLs (homepage + head-keyword pages + top suburb pages).
4. Consider adding `/visiting-cards` to top nav (currently reached via hubs; it's a top-tier landing page in the sitemap with priority 0.95).
